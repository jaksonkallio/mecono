unsigned short int SimNode::neighborCount() const{
	return neighbors.size();
}

bool SimNode::hasNeighbor(std::string neighbor_address) const{
	for(unsigned int i = 0; i < neighborCount(); ++i){
		if
	}
}
